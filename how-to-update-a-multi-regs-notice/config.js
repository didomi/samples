const config = {
  baseUrl: "https://api.didomi.io/v1",
  limit: 5000,
  token: "<token>",

  noticeId: "<noticeId>",
  organizationId: "<organizationId>",
  regulationId: "gdpr",
};

module.exports = config;
